#include <stdio.h>

int main() {
    int marks [90]; // reserved space to store 90 integer
    marks [0]=45;
    marks[1]=95;
    printf("marks 0 and 1 is %d and %d",marks[0], marks[1]);
    return 0;
}